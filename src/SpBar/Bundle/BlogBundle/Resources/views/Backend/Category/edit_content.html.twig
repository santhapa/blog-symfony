{% form_theme form with ['bootstrap_3_horizontal_layout.html.twig','form_errors.html.twig'] %}

<div class="box-body">
	<form class="form-horizontal" method="POST" action="{{ path('sp_blog_category_edit', {'slug': slug}) }}">

		{{ form_label(form.name) }}
		<div class="col-sm-8">
			{{ form_widget(form.name) }}
			{{ form_errors(form.name) }}	
		</div>

		<br><br>
		<div class="form-group">
	        <div class="col-sm-offset-4 col-sm-8">
	            <button type="submit" class="btn btn-info" value="Update">Udate</button>
	        </div>
	    </div>

	</form>
</div><!-- /.box-body -->